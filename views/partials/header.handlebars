<div class="header">
    {{#if editable}}
    <h1 data-field="title" contenteditable="{{editable}}">{{title}}</h1>    
    {{/if}}
    {{#unless editable}}
    <h1>{{title}}</h1>           
    {{/unless}}
</div>